{
  "name": "image-gen",
  "version": "1.0.0",
  "description": "CLI for generating images via an OpenAI-compatible API",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "image-gen": "./src/index.ts"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "openai": "^6.37.0"
  }
}
